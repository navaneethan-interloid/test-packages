global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'nestjs-local-app'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['host.docker.internal:3000'] # ◄ Use this special domain